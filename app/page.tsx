import TransferToken from "./TransferToken";

export default function Home() {
  const tokenContract = "";
  const userAddress = "";
  return (
    <div>
      <TransferToken tokenContract={tokenContract} userAddress={userAddress} />
    </div>
  );
}
